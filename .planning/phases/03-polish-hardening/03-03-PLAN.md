---
phase: 03-polish-hardening
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - config/environments/production.rb
  - DEPLOYMENT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "A new operator can read DEPLOYMENT.md and follow a linear walkthrough from zero to deployed app"
    - "config/environments/production.rb contains commented examples for both sendmail and Gmail SMTP ActionMailer configurations"
    - "config/environments/production.rb has force_ssl/HSTS guidance enabled (uncommented or annotated to match the deployment posture)"
    - "DEPLOYMENT.md documents every placeholder currently in config/deploy.yml that requires operator action"
  artifacts:
    - path: "DEPLOYMENT.md"
      provides: "End-to-end Kamal/Docker deploy walkthrough with mailer + secrets + audit findings"
    - path: "config/environments/production.rb"
      provides: "Annotated mailer + HSTS configuration block"
  key_links:
    - from: "DEPLOYMENT.md"
      to: "config/deploy.yml"
      via: "Operator action items list each placeholder by file:line"
      pattern: "deploy\\.yml"
    - from: "config/environments/production.rb"
      to: "DEPLOYMENT.md"
      via: "Comment block references DEPLOYMENT.md for mailer setup"
      pattern: "DEPLOYMENT\\.md"
---

<objective>
Deliver the deployment-documentation vertical slice: audit `config/deploy.yml` and `Dockerfile`, annotate `config/environments/production.rb` with mailer + HSTS guidance, and write `DEPLOYMENT.md` so a fresh operator can ship the app end-to-end with Kamal.

Purpose: Satisfy D-MAIL-01, D-MAIL-02, D-DEPLOY-01, D-DEPLOY-02. The "user" for this slice is the operator/webmaster; the deliverable they need is a single linear document plus inline production.rb annotations.
Output: One root-level DEPLOYMENT.md, one annotated production.rb, and a human checkpoint confirming the walkthrough is followable.
</objective>

<execution_context>
@/home/cydra/tmp/try-gsd/gsd-notes-app/.claude/get-shit-done/workflows/execute-plan.md
@/home/cydra/tmp/try-gsd/gsd-notes-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish-hardening/03-CONTEXT.md
@.planning/phases/03-polish-hardening/03-PATTERNS.md
@config/deploy.yml
@config/environments/production.rb
@Dockerfile
@CLAUDE.md
</context>

## Phase Goal

**As a** new operator, **I want to** follow a single deployment document plus on-rails production config comments, **so that** I can deploy the notes app to a fresh VPS with Kamal, configure email delivery, and verify the running app — without reading source.

<tasks>

<task type="auto">
  <name>Task 1: Audit deploy.yml + Dockerfile and annotate production.rb with mailer + HSTS guidance</name>
  <files>config/environments/production.rb</files>
  <read_first>
    - config/environments/production.rb (entire file — locate existing SSL comment block at ~line 30 and ActionMailer block at ~line 64)
    - config/deploy.yml (entire file — list every placeholder and configuration gap)
    - Dockerfile (entire file — confirm base image, ENTRYPOINT, exposed port, asset precompile step)
    - .planning/phases/03-polish-hardening/03-PATTERNS.md (lines 231-251 — D-SEC-03 HSTS + D-MAIL-02 sendmail snippet)
    - .planning/phases/03-polish-hardening/03-CONTEXT.md (D-MAIL-01, D-MAIL-02, D-DEPLOY-01, D-DEPLOY-02)
  </read_first>
  <action>
    Edit `config/environments/production.rb` in place. Concrete edits:

    1. **HSTS / SSL block (existing comment block around the `config.assume_ssl` and `config.force_ssl` lines):** Uncomment BOTH `config.assume_ssl = true` and `config.force_ssl = true` (per D-SEC-03 HSTS is delivered by `force_ssl`). Add a one-line comment above each explaining: "Required when deployed behind Kamal's SSL-terminating proxy — see DEPLOYMENT.md." Do NOT add `Strict-Transport-Security` manually; Rails sets it from `force_ssl`.

    2. **ActionMailer block (existing commented SMTP example):** Keep the existing SMTP template (do not delete). ABOVE it, add a comment header explaining the choice is intentional (Phase 3 D-MAIL-01) and that no provider is wired by default. BELOW the existing SMTP block, add a commented `sendmail` alternative:
       - `# --- Sendmail (Linux VPS default — simplest) ---`
       - `# config.action_mailer.delivery_method = :sendmail`
       - `# config.action_mailer.sendmail_settings = { location: "/usr/sbin/sendmail", arguments: "-i" }`
       - `# config.action_mailer.perform_deliveries = true`
       - `# config.action_mailer.raise_delivery_errors = true`
       And add a commented Gmail SMTP example showing the existing template filled with placeholder values (`address: "smtp.gmail.com"`, `port: 587`, `user_name: ENV["SMTP_USERNAME"]`, `password: ENV["SMTP_PASSWORD"]`, `authentication: :plain`, `enable_starttls_auto: true`).
       After all mailer comments, add: `# See DEPLOYMENT.md for full mailer configuration walkthrough.`

    3. Do NOT touch the existing CSP, asset, or logging configuration in this file.
    4. Do NOT add any code outside comment lines for mailer (delivery stays unconfigured per D-MAIL-01).

    While performing this edit, record the audit findings (deploy.yml placeholders, Dockerfile observations) in your scratch notes — they feed Task 2.
  </action>
  <verify>
    <automated>cd /home/cydra/tmp/try-gsd/gsd-notes-app && grep -E '^[[:space:]]*config\.force_ssl[[:space:]]*=[[:space:]]*true' config/environments/production.rb && grep -E '^[[:space:]]*config\.assume_ssl[[:space:]]*=[[:space:]]*true' config/environments/production.rb && grep -q 'Sendmail' config/environments/production.rb && grep -q 'smtp.gmail.com' config/environments/production.rb && grep -q 'DEPLOYMENT.md' config/environments/production.rb && bin/rails runner -e production 'puts Rails.application.config.force_ssl' 2>&1 | tail -1 | grep -qx true</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `config.force_ssl = true` and `config.assume_ssl = true` appear UNCOMMENTED in production.rb.
    - Source assertion: production.rb contains the strings "Sendmail", "smtp.gmail.com", and "DEPLOYMENT.md".
    - Source assertion: every new mailer line (sendmail/gmail examples) is prefixed with `#` — no live delivery configuration is activated.
    - Behavior: `bin/rails runner -e production 'puts Rails.application.config.force_ssl'` prints `true` (boot succeeds and the value flips).
    - Behavior: `bin/rails test` (default test env) still exits 0 — production-only edits do not affect test boot.
  </acceptance_criteria>
  <done>production.rb annotated; HSTS active in production env; audit findings captured for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write DEPLOYMENT.md walkthrough including deploy.yml audit findings</name>
  <files>DEPLOYMENT.md</files>
  <read_first>
    - config/deploy.yml (re-read to enumerate every placeholder by line number)
    - Dockerfile (confirm exposed port, build args, ENV vars consumed)
    - bin/docker-entrypoint (confirm db:prepare behavior referenced in deploy steps)
    - config/environments/production.rb (post-Task 1 — mirror the mailer options documented inline)
    - CLAUDE.md (Encryption Keys setup section — DEPLOYMENT.md must reference the credentials.yml.enc + RAILS_MASTER_KEY requirement)
    - .planning/phases/03-polish-hardening/03-CONTEXT.md (D-DEPLOY-02 walkthrough checklist)
  </read_first>
  <action>
    Create `DEPLOYMENT.md` at the repo root with the following sections in order. Use plain Markdown, no fenced code blocks longer than ~10 lines, no emojis.

    1. **Title + 1-paragraph intro** stating this app deploys via Kamal + Docker to a single Linux VPS running SQLite + Solid Queue/Cache/Cable.

    2. **Prerequisites** — bullet list: Docker installed locally, Kamal CLI (`gem install kamal` or `bundle exec kamal`), SSH access to the target server, a container registry account (Docker Hub / GHCR / DO Registry), a Linux VPS reachable over SSH on port 22, the `RAILS_MASTER_KEY` from `config/master.key`.

    3. **Encryption keys** — restate the CLAUDE.md "Setup: Encryption Keys" steps abbreviated (5 numbered steps), and emphasize that `config/master.key` must NEVER be committed but MUST be available to the deploy host via Kamal secrets.

    4. **Container registry setup** — concrete instructions for the two most common cases (Docker Hub login via `docker login` and GitHub Container Registry login via `docker login ghcr.io -u USERNAME -p TOKEN`). Note where the registry credentials plug into `config/deploy.yml` (`registry.server`, `registry.username`, `registry.password` — currently placeholders).

    5. **Audit: deploy.yml placeholders to replace before first deploy** — a table listing every placeholder found in `config/deploy.yml`, with columns: `Key` | `Current Value` | `Action Required`. Must include at minimum:
       - `servers.web` — `192.168.0.1` — Replace with target VPS IP or DNS name.
       - `registry.server` — `localhost:5555` — Replace with chosen registry hostname (e.g. `ghcr.io`, `registry.hub.docker.com`).
       - `registry.username` / `registry.password` — commented — Uncomment and supply, or move to `.kamal/secrets`.
       - `proxy.ssl` / `proxy.host` — Confirm the public hostname matches DNS; SSL via Let's Encrypt requires the host to be reachable on 443.
       - Any other placeholder discovered in the audit.

    6. **Secrets & environment variables** — list every env var the production stack consumes: `RAILS_MASTER_KEY` (required), `SMTP_USERNAME` / `SMTP_PASSWORD` (only if Gmail SMTP chosen), `KAMAL_REGISTRY_PASSWORD` (Kamal convention). Show where each lives (`.kamal/secrets` or shell env when running `kamal deploy`).

    7. **Mailer configuration** — two subsections matching production.rb comments:
       - 7a. **Sendmail (simplest for Linux VPS)** — three numbered steps: install sendmail on the host, uncomment the sendmail block in `config/environments/production.rb`, redeploy with `kamal deploy`. Note: only works for outbound mail from the VPS itself.
       - 7b. **Gmail SMTP** — four numbered steps: create a Google App Password, set `SMTP_USERNAME` / `SMTP_PASSWORD` via `.kamal/secrets`, uncomment the Gmail block in production.rb, redeploy.

    8. **First deploy command sequence** — numbered commands:
       1. `bundle exec kamal setup` (first time only — provisions Docker on the server, sets up the registry login, deploys the proxy + app).
       2. `bundle exec kamal deploy` (subsequent deploys).
       3. `bundle exec kamal app logs -f` (verify boot).
       4. `bundle exec kamal console` (open a Rails console on the deployed app — use this to create the first user since self-registration is disabled per CLAUDE.md / D-AUTH).

    9. **Creating the first user (webmaster-only flow)** — show the Rails console command exactly: `User.create!(email: "you@example.com", password: "longrandompassword")` and note that this is the ONLY user-creation path (no self-registration UI exists).

    10. **Verifying the running app** — checklist: hit `https://YOUR_HOST/up` and expect HTTP 200; hit `https://YOUR_HOST/users/sign_in` and expect the styled sign-in page; open DevTools Network tab and confirm `X-Frame-Options`, `X-Content-Type-Options`, `Content-Security-Policy`, and `Strict-Transport-Security` headers are present.

    11. **Common issues** — short FAQ: missing `RAILS_MASTER_KEY` → app fails to boot; missing encryption keys in credentials → "MissingKey" error; SQLite volume not mounted → notes vanish across deploys (mount `gsd_notes_app_storage:/rails/storage`).

    Final file length target: 200-400 lines of markdown. No external links to documentation unless absolutely required (this is a self-contained operator doc).
  </action>
  <verify>
    <automated>cd /home/cydra/tmp/try-gsd/gsd-notes-app && test -f DEPLOYMENT.md && wc -l DEPLOYMENT.md | awk '{exit ($1 < 100) ? 1 : 0}' && grep -q '^## Prerequisites' DEPLOYMENT.md && grep -q 'RAILS_MASTER_KEY' DEPLOYMENT.md && grep -q 'kamal deploy' DEPLOYMENT.md && grep -q 'kamal setup' DEPLOYMENT.md && grep -q 'sendmail' DEPLOYMENT.md && grep -q 'smtp.gmail.com' DEPLOYMENT.md && grep -q 'User.create!' DEPLOYMENT.md && grep -q '192.168.0.1' DEPLOYMENT.md && grep -q 'localhost:5555' DEPLOYMENT.md && grep -q '/up' DEPLOYMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - Artifact: `DEPLOYMENT.md` exists at repo root with at least 100 lines.
    - Source assertion: file contains all 11 section headings listed in the action (verifiable by `grep -E '^##' DEPLOYMENT.md` returning at least 10 H2 entries).
    - Source assertion: deploy.yml audit table mentions `192.168.0.1`, `localhost:5555`, and at least 2 other placeholders by name.
    - Source assertion: file documents both `sendmail` and `smtp.gmail.com` mailer options matching the production.rb comments from Task 1.
    - Source assertion: file shows the exact `User.create!` command for first-user creation (no self-registration).
    - Source assertion: file references `/up`, `X-Frame-Options`, and `Content-Security-Policy` in the verification checklist.
  </acceptance_criteria>
  <done>DEPLOYMENT.md present, comprehensive, internally consistent with production.rb annotations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator-perspective review of DEPLOYMENT.md walkthrough</name>
  <files>DEPLOYMENT.md, config/environments/production.rb</files>
  <read_first>
    - DEPLOYMENT.md (post-Task 2)
    - config/environments/production.rb (post-Task 1)
    - config/deploy.yml (for audit cross-check)
  </read_first>
  <action>
    Halt automated execution and present the walkthrough to the human operator for review. Do not modify files in this task — its purpose is human sign-off on the Task 1 + Task 2 outputs against the criteria in <how-to-verify>. If the operator returns gaps, treat each gap as a follow-up edit (re-enter Task 1 or Task 2 scope as appropriate) and re-request approval.
  </action>
  <what-built>
    DEPLOYMENT.md walkthrough (Kamal + Docker deploy, mailer setup, deploy.yml audit) plus annotated config/environments/production.rb with HSTS + mailer comments. No new functional code — pure operator documentation slice.
  </what-built>
  <how-to-verify>
    1. Open `DEPLOYMENT.md` and read it top-to-bottom as if you had never deployed this app.
    2. Confirm every step is concrete enough that you could execute it without re-reading source files. Flag any ambiguous step.
    3. Cross-check the deploy.yml audit table against `config/deploy.yml` — every placeholder in the YAML should appear in the table.
    4. Confirm the mailer section matches `config/environments/production.rb` comments exactly (same provider names, same env var names, same `delivery_method` symbols).
    5. Confirm the first-user creation command matches the project's no-self-registration constraint (`User.create!` with email + password).
    6. Confirm the verification checklist (Section 10) lists the headers actually delivered by Plan 03-02 (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Content-Security-Policy, Strict-Transport-Security).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 3 documentation complete, or describe gaps (e.g. "mailer section missing port for Gmail", "audit table missed proxy.host") so the executor can patch.</resume-signal>
  <verify>
    <automated>echo human-checkpoint</automated>
    <human-check>Operator reads DEPLOYMENT.md end-to-end and applies the 6-point check in <how-to-verify>; responds with "approved" or a list of gaps.</human-check>
  </verify>
  <acceptance_criteria>
    - Human reviewer signal received with literal string "approved".
    - If gaps reported, they are addressed and a fresh approval is obtained before proceeding.
  </acceptance_criteria>
  <done>Operator has signed off with "approved" on the DEPLOYMENT.md walkthrough.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Deploy host | DEPLOYMENT.md guides handling of `RAILS_MASTER_KEY`, registry credentials, SMTP secrets — any mistake leaks secrets |
| Git repo → public | Documentation must not embed real secrets; every secret value must be a placeholder or env-var reference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Information disclosure | DEPLOYMENT.md accidentally containing real RAILS_MASTER_KEY or SMTP password | mitigate | Doc uses only placeholder values (`ENV["SMTP_USERNAME"]`, `you@example.com`) — Task 2 verification grep explicitly checks no `.key` blob is embedded. Human checkpoint (Task 3) re-verifies. |
| T-03-13 | Tampering | Operator following stale instructions and shipping with `force_ssl = false` | mitigate | Task 1 uncomments `config.force_ssl = true` in code; DEPLOYMENT.md verification step asserts HSTS header presence post-deploy. |
| T-03-SC | Tampering | Package installs during deploy | accept | No new package installs in this plan (Gemfile unchanged); Dockerfile audit is read-only. Phase 1/2 already cleared the dependency tree. |
</threat_model>

<verification>
- Automated: Task 1 grep chain confirms production.rb annotations.
- Automated: Task 2 grep chain confirms DEPLOYMENT.md structure and key strings.
- Automated: `bin/rails runner -e production 'puts Rails.application.config.force_ssl'` returns `true`.
- Human: Task 3 checkpoint — operator-perspective walkthrough review.
</verification>

<success_criteria>
- DEPLOYMENT.md exists at repo root, ≥100 lines, covers all 11 sections from the action.
- production.rb has uncommented `force_ssl` and `assume_ssl`, plus both sendmail and Gmail SMTP commented examples.
- Human reviewer (Task 3) signs off with "approved".
- ROADMAP Phase 3 readiness for production deploy is documented (D-DEPLOY-01, D-DEPLOY-02, D-MAIL-01, D-MAIL-02 all addressed).
</success_criteria>

<output>
Create `.planning/phases/03-polish-hardening/03-03-SUMMARY.md` when done.
</output>
